Just a simple lil' chat app to allow concurrent messaging from users sharing a server.

All messages are saved to a database using SQLAlchemy and displayed when the user loads the interface.

Built with Flask, SQLAlchemy, and Socket.io